<%= form_for @order do |f| %>
  <%= f.label :user, "User:" %>
  <%= f.collection_select :user, User.all, :id, :name %>
  <%= f.label :cake, "Cake:" %>
  <%= f.collection_select :cake, Cake.all, :id, :name %>
  <%= f.submit "Place Order" %>
<% end %>
